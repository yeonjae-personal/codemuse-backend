<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ui-multi-lang-label-m">

  <select id="findByProperties" resultType="com.lgcns.svcp.prod.entity.MultiLangLabelEntity">
    select t.label_id, t.lang_code, t.region_code, t.label_name, t.label_type,
    t.rgst_user, t.rgst_dtm, t.upd_user, t.upd_dtm, t.label_dscr
    FROM tb_multi_lang_label_m t where t.label_type = 'Additional'
    <if test="langCode != null">
    	and t.lang_code = #{langCode}
    </if>
    <if test="labelId != null">
	    and t.label_id in
	    (select distinct label_id FROM tb_multi_lang_label_m where lower(label_id) like <![CDATA[ '%' || #{labelId} || '%']]> and label_type = 'Additional')
    </if>
    <if test="labelName != null">
	    and t.label_id in
	    (select distinct label_id FROM tb_multi_lang_label_m where lower(label_name) like <![CDATA[ '%' || #{labelName} || '%']]> and label_type = 'Additional')
    </if>
    order by t.label_id desc
  </select>

  <select id="findByLabelGetAll" resultType="com.lgcns.svcp.prod.ui.prod.dto.label.LabelItemDto">
    SELECT lang.lang_code, lang.region_code, t.label_name, t.label_dscr
    FROM tb_lang_m lang
    LEFT JOIN tb_multi_lang_label_m t ON lang.lang_code = t.lang_code 
        AND t.label_id = #{labelId} 
        AND t.label_type = 'Additional'
    WHERE lang.use_yn = 'Y'
    ORDER BY lang.sort_no ASC
  </select>
  
  <select id="findByLabelId" resultType="com.lgcns.svcp.prod.ui.prod.dto.label.LabelItemDto">
    select t.lang_code, t.region_code, t.label_name, t.label_dscr
    FROM tb_multi_lang_label_m t where t.label_id = #{labelId} and t.label_type = 'Additional'
  </select>

  <insert id="insert">
	INSERT INTO tb_multi_lang_label_m
    (label_id, lang_code, region_code, label_name, label_type, label_dscr, rgst_user, rgst_dtm, upd_user, upd_dtm)
    VALUES (#{labelId}, #{langCode}, #{regionCode}, #{labelName}, #{labelType}, #{labelDscr},
    		#{rgstUser}, CURRENT_TIMESTAMP, #{updUser}, CURRENT_TIMESTAMP)
  </insert>
  
  <insert id="insertLabelSystem">
	INSERT INTO tb_multi_lang_label_m
    (label_id, lang_code, region_code, label_name, label_type, rgst_user, rgst_dtm, upd_user, upd_dtm)
    VALUES (#{labelId}, #{langCode}, #{regionCode}, #{labelName}, #{labelType},
    		#{rgstUser}, CURRENT_TIMESTAMP, #{updUser}, CURRENT_TIMESTAMP)
  </insert>

  <update id="update">
	UPDATE tb_multi_lang_label_m
    <set>
    label_name = #{labelName},
    label_dscr = #{labelDscr},
    upd_user = #{updUser},
    upd_dtm = CURRENT_TIMESTAMP
    </set>
    WHERE label_id = #{labelId} and lang_code = #{langCode} and region_code = #{regionCode} and label_type = 'Additional'
  </update>

  <delete id="delete">
	DELETE
    FROM tb_multi_lang_label_m
    WHERE label_id = #{labelId} and label_type = 'Additional'
  </delete>

  <delete id="deleteInUpdate">
	DELETE
    FROM tb_multi_lang_label_m
    WHERE label_id = #{labelId} and lang_code = #{langCode} and region_code = #{regionCode}
    and label_type = 'Additional'
  </delete>

  <select id="findLabelIds" resultType="java.lang.String">
    SELECT label_id
    FROM tb_multi_lang_label_m t where t.label_type = 'Additional'
    order by label_id desc
  </select>

  <select id="findLanguageI18n" resultType="com.lgcns.svcp.prod.entity.MultiLangLabelEntity">
    select t.label_id, t.label_name, t.lang_code, t.label_dscr
    FROM tb_multi_lang_label_m t where t.label_type = 'Additional' or t.label_type is null or t.label_type = ''
  </select>

  <select id="checkLabelExistImportExcel" resultType="int">
    SELECT count(*)
    FROM tb_multi_lang_label_m t where t.label_id = #{labelId} and t.lang_code = #{langCode} and t.region_code = #{regionCode}
  </select>

  <update id="updateFromExcel">
	UPDATE tb_multi_lang_label_m
    <set>
    label_name = #{labelName},
    upd_user = #{updUser},
    upd_dtm = CURRENT_TIMESTAMP
    </set>
    WHERE label_id = #{labelId} and lang_code = #{langCode} and region_code = #{regionCode}
  </update>

</mapper>